{
  "name": "aim-takehome",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "postinstall": "cd src/client && pnpm install && cd ../server && pnpm install",
    "start-client": "cd src/client && pnpm start",
    "start-server": "cd src/server && pnpm start",
    "start": "pnpm install && concurrently pnpm:start-client pnpm:start-server"
  },
  "keywords": [],
  "author": "Logan Zartman",
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
